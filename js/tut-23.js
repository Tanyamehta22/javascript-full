console.log("welcome to tutorial number 23");
let x=3;
let  y=6;
let z;
z= x+y;
z= x-y;
z= x*y;
z= x/y;

// exploring the math object
z=Math;
z=Math.PI;
z=Math.E;
z=Math.round(5.4);
z= Math.ceil(5.3); // will round off to upper value
z= Math.floor(-5.3); //will round off to lower value 
z= Math.abs(5); // absolute value
z= Math.sqrt(64); // square root
z= Math.pow(2,3); // power
z= Math.min(2,345,67,23); //minimum
z= Math.max(2,345,14,67); //maximum
z= Math.random(); // will generate random no.
z= 100*Math.random();//will get a random value bet. 0-100
z= 50+(100-50)*Math.random(); //when we want value between 50-100
z= Math.ceil(50+(100-50)*Math.random());// will generate absolute value

console.log(z); 
 